// Accessing object properties with dot notation

/* Using the dot notation, this appends an objects property name after then
object in order to store it in a new variable. */
// Setup
var testObj = {
  "hat": "ballcap",
  "shirt": "jersey",
  "shoes": "cleats"
};

// Only change code below this line

var hatValue = testObj.hat;      // Change this line
var shirtValue = testObj.shirt;    // Change this line
