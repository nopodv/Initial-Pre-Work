// Generate random fractions

// Adds function to give a random number.
function randomFraction() {

  // Only change code below this line.
Math.random();
  return Math.random();

  // Only change code above this line.
}
