// Iterate with for loops

/* Creates a loop to to add a number to an array "for" a specified number of
times. Then it will stop. */
// Example
var ourArray = [];

for (var i = 0; i < 5; i++) {
  ourArray.push(i);
}

// Setup
var myArray = [];

// Only change code below this line.
for (var i = 1; i < 6; i++) {
  myArray.push(i);
}
