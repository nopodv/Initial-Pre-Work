// Use the conditional operator

/* Creates and if else statement in one line. It streamlines the function
more or less. */
function checkEqual(a, b) {
  return a == b ? true : false;
}

checkEqual(1, 2);
