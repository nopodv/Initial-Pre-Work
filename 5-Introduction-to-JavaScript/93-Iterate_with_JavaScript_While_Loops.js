// Iterate with while loops

/* This while loop can run continuously until it reaches a point that it has
finished processing all the statments. */
// Setup
var myArray = [];

// Only change code below this line.
var i = 0;
while (i < 5) {
  myArray.push(i);
  i++;
}
