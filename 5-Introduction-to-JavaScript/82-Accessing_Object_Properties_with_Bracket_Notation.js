// Accessing object properties with bracket notation

/* Using the bracket notation, this appends an objects property name after the
object in order to store it in a new variable. However brackets are used due
to a property name having more than one word with a space. */
// Setup
var testObj = {
  "an entree": "hamburger",
  "my side": "veggies",
  "the drink": "water"
};

// Only change code below this line

var entreeValue = testObj["an entree"];   // Change this line
var drinkValue = testObj["the drink"];    // Change this line
