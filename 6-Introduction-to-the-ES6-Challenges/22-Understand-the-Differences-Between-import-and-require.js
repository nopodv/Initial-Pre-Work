// Undertand the differences between important and require

/* Imports only selective info from an external source as opposed to the whole
of the files themselves. This is helpful when needing only a small section of
the data. */
"use strict";
import { capitalizeString } from "string_functions"
